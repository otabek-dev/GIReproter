stages:
  - build
  - migration
  - start

build:
  tags:
  - dev
  stage: build
  script:
    - dotnet build
    - pwd
    - ls -all

migration:
  tags:
  - dev
  stage: migration
  script:
    - pwd
    - ls -all
